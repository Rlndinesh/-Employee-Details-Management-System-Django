# -Employee-Details-Management-System-Django
This is a Django-based web application for managing and displaying employee details.   The project includes an `employees` app, HTML templates, media handling, and uses SQLite as the default database.
